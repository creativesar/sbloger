/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'cdn.rareblocks.xyz', 'example.com', 'images.squarespace-cdn.com', 'readymadeui.com'],
  },
};

export default nextConfig;
